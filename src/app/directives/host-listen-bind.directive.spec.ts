import { HostListenBindDirective } from './host-listen-bind.directive';

describe('HostListenBindDirective', () => {
  it('should create an instance', () => {
    const directive = new HostListenBindDirective();
    expect(directive).toBeTruthy();
  });
});
